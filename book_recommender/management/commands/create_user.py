from django.core.management.base import BaseCommand
from book_recommender.models import User

'''class Command(BaseCommand):
    Helper function to load data into neo4j database
    help = 'creates new user node'

    def handle(self):
'''